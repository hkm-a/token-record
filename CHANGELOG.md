# 变更记录

## 1.6.3 — 2026-07-28

### 修复

- **拖拽卡顿（根因）**：`get_snapshot` 此前是同步命令，在主线程上每 2 秒全量重读并解析
  各工具会话目录（本机实测约 500MB / 单次 5.2s），Win32 消息循环被持续阻塞。
  现改为 `async` + `spawn_blocking` 移出主线程，并为逐文件解析增加 (mtime, size)
  缓存——未变化的文件不再重读，热刷新实测 5.2s → 0.12s（约 45 倍）。
- **折叠死区（根因）**：`setSize` 等窗口调用传入普通对象会被序列化为
  `{"undefined": …}`，Rust 端反序列化失败、调用静默无效——窗口从未真正缩放，
  始终保持初始 680px 高，内容下方的透明区域挡住鼠标，形成"死区"。
  现改传 `LogicalSize` 实例，窗口高度真实贴合内容。
- **窗口尺寸抖动**：`fitContent` 此前每 tick 连发 6 次窗口调用
  （setResizable/setMinSize/setSize/setMaxSize/SetWindowPos），且展开时被上次
  残留的 `maxSize` 钳制。现在高度未变化时零窗口操作；窗口保持 `resizable: false`，
  程序化缩放不需要 resizable/min/max 任何配合，相关调用与权限一并移除。
- 刷新逻辑加串行锁，轮询与手动刷新并发触发时不再重复扫盘。

### 变更

- 版本 1.6.2 → 1.6.3；`get_version` 与默认偏好版本改由 `CARGO_PKG_VERSION` 派生。
- 新增回归测试 `refresh_bench.rs`：断言缓存前后聚合结果一致。

## 1.6.2 — 2026-07-28

### 修复

- **拖拽卡顿**：`backdrop-filter` 从 blur(28px) 降为 blur(8px)，拖拽期间自动禁用滤镜，
  添加 `will-change: transform` GPU 加速
- **折叠死区**：折叠后窗口下方区域无法点击的问题，通过 `setMinSize` + 两步 resize
  强制 Windows DWM 刷新窗口可见区域
- **性能**：日常 backdrop-filter 强度降低 70%（28→8px），减少帧率开销

### 变更

- 版本 1.6.1 → 1.6.2

## 1.6.1 — 2026-07-28
### 安全与健壮性

- **CSP 策略**：从 null（无限制）改为 restrictive CSP，限制 script/style/connect 来源
- **权限收敛**：移除不必要的 `shell:allow-open` 权限，仅保留 `core:default` + `updater:default`
- **panic 防护**：`home_dir()` unwrap 改为 fallback `.`
- **panic 防护**：`pricing.rs` 解析 pricing.json 失败时返回默认定价而非 panic
- **delta 安全**：`compute_delta` 中 `cost_delta` 用 `.max(0.0)` 防止负值

### 代码清理

- 移除未使用的 `tauri-plugin-shell` 依赖
- 移除未使用的 `get_app_info` 命令
- 移除 `refreshNow` 中 dispatch CustomEvent 的死代码
- 移除根目录重复的 `latest.json`
- `pricing.rs` 的 `get_entry` 改用 `match_model` 消除重复子串匹配逻辑

### 测试

- Rust 集成测试 15/15 通过
- JS 核心测试 27/27 通过

## 1.6.0 — 2026-07-28

### 重构

- **Tauri v2 迁移完成**：从 Electron（63MB）迁移至 Tauri v2（3.8MB），体积缩小 17×
- **Rust 后端**：采集器、聚合器、定价计算全部用 Rust 重写，编译时嵌入 pricing.json
- **前端桥接**：`tauri-bridge.js` 提供 `window.api` 兼容接口，`app.js` 零改动
- **Delta 增量**：后端计算快照 delta，仅 token 增量 > 0 时前端弹气泡，消除鬼畜刷新
- **透明悬浮窗**：`transparent: true` + `shadow: false` + `decorations: false`
- **自动更新**：集成 `tauri-plugin-updater`，签名密钥对已生成
- **Rust CLI**：`trcli` 二进制（569KB），输出与 JS CLI 一致
- **垃圾清理**：删除全部 Electron 运行时代码（main/collectors/cli/store/files/paths）
- **垃圾清理**：删除 electron-builder 配置、afterPack.js、launch.cmd
- **垃圾清理**：删除 32 个 Electron 依赖测试，保留 27 个核心逻辑测试
- **双托盘修复**：删除 tauri.conf.json trayIcon 配置（与代码手动创建重复）

### 测试

- Rust 集成测试 15/15 通过
- JS 核心测试 27/27 通过（aggregator/calculator/csv/date/history）


### 优化

- **自动更新重构**：修复 v2.0.0-test 标签被当作最新版的问题，改用严格 semver 过滤
- **自动更新**：下载过程实时显示进度（版本号处显示百分比），完成后显示「↻重启」
- **自动更新**：点击版本号先弹确认对话框再下载，下载完确认后再重启
- **Bug 修复**：修复悬浮窗刷新按钮无效的问题（缺失 IPC handler）
- **性能**：采集器改为并行 I/O，多数据源时刷新速度大幅提升
- **性能**：fileCache 限制 500 条目上限，防止长期使用内存膨胀
- **重构**：提取 shared/date.js 消除 localDayKey 重复定义
- **重构**：拆分 Store 职责，提取 history.js / csv.js 独立模块
- **健壮性**：HTTP 重定向增加 5 次上限，防止无限循环
- **健壮性**：静默 catch 分支添加 console.warn，便于问题定位
- **UI**：窗口隐藏时不播放收银音效
- **测试**：新增 date / history / csv / updater 单元测试（共 18 个）
- **测试**：全部 59 个测试通过
## 1.5.7 — 2026-07-27

### 新增
- 版本号闪烁检测到更新后自动弹出确认对话框（非静默模式下可直接更新）

### 修复
- 修复版本号闪烁通知链路验证：现确认 silent 模式返回结果后调用方能正确发送 update-available 事件到渲染层
- 新增环境变量 TOKENREC_SIMULATE_UPDATE=1 可强制模拟有新版本用于调试 UI

## 1.5.6 — 2026-07-27

### 改进
- 托盘菜单极致压缩：显/隐 → 刷新 → 导出 → 定价 → 更新 → 自启 → 退出，宽度大幅收窄

## 1.5.5 — 2026-07-27

### 修复
- 修复版本号闪烁从未生效的 bug：静默检查发现新版本时不再弹原生对话框，改为通知悬浮窗 UI 闪烁版本号，点击后自动更新

## 1.5.4 — 2026-07-27

### 改进
- 托盘菜单进一步收窄：去掉顶部装饰标题（版本号 tooltip 已显示），最宽项从「Token 记录 v1.5.3」降到「显示 / 隐藏」

## 1.5.3 — 2026-07-27

### 改进
- **悬浮窗版本号更新提示**：检测到新版本时版本号变金色闪烁，右侧小圆点呼吸，点击即自动更新
- 更新流程：点版本号 → 静默下载 → 替换 → 自动重启，全程无需手动操作

## 1.5.2 — 2026-07-27

### 改进
- **一键自动更新**：点「下载并更新」→ 自动下载 exe → 老进程退出 → 批处理替换 → 自动重启新版本
- 更新流程不再手动操作，体验与常规软件一致

## 1.5.1 — 2026-07-27

### 新增
- **Pi 采集**：新增 `~/.pi/agent/sessions/**/*.jsonl` 数据源，支持 Pi 的 token 与缓存统计
- Pi 卡片在 UI 中位列第三（Claude → Codex → Pi → Grok）
- **Codex 按日拆分修复**：跨日 sessions 的 token 按事件实际发生日归因，修复今日 token 虚高问题
- 定价新增 Pi 模型（GLM-5.2、deepseek-v4-flash、mimo-v2.5 等）

### 变更
- 升级 v1.4.5 → v1.5.0
- README / CLI / 空态提示 四源同步
- 四源采集器引用、状态检查、注释全面对齐

## 1.5.1 — 2026-07-27

### 改进
- 托盘菜单缩短标签："导出 CSV…"→"导出…"、"打开价目覆盖文件"→"价目覆盖"、"检查更新…"→"检查更新"
- 菜单整体更窄，不再撑宽

## 1.4.5 — 2026-07-27

### 折叠态
- 最小化后**只保留**总 Tokens / 总花费两个大数字（+ 标题栏）
- 隐藏 7 日柱、工具卡、状态栏与周期副行；窗口贴内容高度

## 1.4.4 — 2026-07-24

- 窗口高度跟随内容，消除卡片与状态栏之间空白

## 1.4.3 — 2026-07-24

- 去掉「今日/近7日」大卡片，并入总览副行；7 日改为单行火花条
- 模型名回到卡片二级行；信息密度提高

## 1.4.2 — 2026-07-24

- 「今日」移到卡片右上角（原模型名位置）；卡片更矮

## 1.4.1 — 2026-07-24

### 界面（明显放宽）
- 窗口 **500×880**；卡片脚注改为「入/出/存」芯片，不再挤成一行乱码感
- 今日/近7日费用与 token **分两行**显示
- 卡片/标题/状态栏留白再加大

## 1.4.0 — 2026-07-24

### 新增
- **自动更新**：启动静默检查 GitHub Releases；托盘「检查更新…」可下载便携包或打开发布页

### 界面
- 窗口加宽加高（460×780），区块/卡片内边距与间距加大，缓解拥挤感
- 周期迷你柱加高；卡片区间隙与字号微调

## 1.3.0 — 2026-07-24

### 新增
- 采集源健康检查：`snapshot.sources`（目录缺失 / 无会话 / 正常）
- 悬浮窗空态横幅与卡片级源状态提示
- CLI 打印各数据源路径与状态
- README 产品边界、发布与 30 秒上手

### 发布
- Windows 便携包 `TokenRecord-1.3.0-portable.exe`

## 1.2.0 — 2026-07-24

- 系统托盘真图标与完整菜单
- 关窗进托盘、窗口位置记忆、开机自启
- electron-builder 便携打包

## 1.1.0 — 2026-07-24

- 按日聚合 `byDay`、今日 / 近 7 日 `period`
- CSV 导出、`daily.json` 历史
- 用户价目覆盖 `pricing.override.json`
- UI 今日/近 7 日与迷你柱

## 1.0.0

- 三源采集、计价、悬浮窗、动效与 CLI
